# Voxtral Bridge Verda Test

Minimal source bundle for running the Voxtral Hyprnote bridge inside Verda using the public `vllm/vllm-openai:nightly` base image.

This repo intentionally contains no runtime credentials.
